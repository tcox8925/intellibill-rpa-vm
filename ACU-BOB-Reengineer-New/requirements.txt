# ACU/BOB Data Pipeline — Python dependencies
# Install: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# Requires: Python 3.10+ (tested on 3.14), Azure CLI login (`az login`) for Key Vault / Blob / DB auth

# ── Core data processing ──
pandas==3.0.3
numpy==2.4.6
python-dateutil==2.9.0.post0
pytz==2025.2

# ── Database ──
psycopg2-binary==2.9.12
pyodbc==5.3.0

# ── Azure (Blob, Key Vault, Email, identity) ──
azure-identity==1.25.3
azure-keyvault-secrets==4.11.0
azure-storage-blob==12.30.0
azure-communication-email==1.1.0

# ── AI (run reports, carrier mapper) ──
openai==2.41.1

# ── File formats (CSV / Excel / encrypted Excel / HTML fallback) ──
openpyxl==3.1.5
xlrd==2.0.2
msoffcrypto-tool==6.0.0
lxml==6.1.1
html5lib==1.1
beautifulsoup4==4.13.4

# ── Optional: mapping review UI (scripts/mapping_server.py) ──
Flask==3.1.3
fastapi==0.115.12
uvicorn==0.34.0
